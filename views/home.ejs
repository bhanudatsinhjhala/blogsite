<%- include("partials/header") -%>

    <h1>Home</h1>

    <p>
        <%= homeStartingContent %>
    </p>
    <% posts.forEach(function(post){ %>
        <h1>
            <%= post.title %>
        </h1>
        <p>

            <%= post.description.substring(0,100) %>....<br>
                <a href="/posts/<%= post._id %>">ReadMore</a>
        </p>
        <% }); %>

            <%- include("partials/footer") -%>